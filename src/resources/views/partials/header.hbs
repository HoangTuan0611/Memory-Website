<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">Memory</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a id="username" class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <img src="http://sofmie.com/public/img/dalat.jpeg" alt="" class="user-avatar">
            Guest
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a id='post-video' class="dropdown-item" href="/videos/create">Đăng video</a>
            <div class="dropdown-divider"></div>
            <a id='profile' class="dropdown-item" href="/me/user/profile">Profile</a>
            <a id='videos' class="dropdown-item" href="/me/stored/videos">Video của tôi</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" onclick="Logout()" href="/auth">Đăng xuất</a>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
<script>
  var slug = localStorage.getItem('slug');
  var userslug = localStorage.getItem('slug');
  document.getElementById('profile').setAttribute('href', '/me/user/profile/' + slug);
  document.getElementById('videos').setAttribute('href', '/me/stored/videos/' + userslug);
  function Logout(){
      localStorage.removeItem('slug');
  }
  window.onload = function(){
    if(!localStorage.getItem('slug') || !localStorage.getItem('fullname')){
      localStorage.setItem('slug', 'Guest');
      localStorage.setItem('fullname', 'Guest');
    }
    checkedLogin();
  }
  function checkedLogin(){
    var isLogin = localStorage.getItem('slug');
    var Login = localStorage.getItem('fullname');
    console.log(isLogin);
    console.log(Login);
    if(isLogin != 'Guest'){
      document.getElementById('username').innerHTML = '<img src="http://sofmie.com/public/img/dalat.jpeg" alt="" class="user-avatar"> ' + Login;
    }
    else{
      if (typeof isCheckLogin !== "undefined" && isCheckLogin){
        checkLogin();
      }
    }
  }
</script>
